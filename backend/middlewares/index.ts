import { boomErrorHandler, errorHandler, logErrors } from "./errorHandler";
import { validatorHandler } from "./schemaHandler";
import { success, error } from "./response";

export default {
    boomErrorHandler,
    errorHandler,
    logErrors,
    validatorHandler,
    success,
    error
}